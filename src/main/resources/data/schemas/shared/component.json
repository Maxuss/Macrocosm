{
    "$id": "component",
    "title": "MiniMessage text component",
    "description": "A MiniMessage compatible string containing the component definition",
    "type": "string",
    "pattern": "[^<>]*(<([^<>]+|[^<>]+\"[^\"]+\")>)*[^<>]*",
    "examples": [
        "<red>Red text",
        "<hover:show_text:\"abc\">Hover text!",
        "<rainbow>Rainbow text!"
    ]
}
